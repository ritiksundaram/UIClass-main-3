Instructions to run the file:

(Unzip the file)

1. Go to folder "UIClass/backend" and run the backend by typing "python main.py"
2. Open a new terminal window and go to the folder "UIClass/frontend" and install any dependencies by typing "npm install"
3. Run the app by typing "npm start"

